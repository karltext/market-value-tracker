package com.example.MarketSystem;

import org.springframework.boot.SpringApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.example.MarketSystem.MarketTrackerApplication;

public class MarketTrackerApplication {

	
	public static void main(String[] args) {
		ConfigurableApplicationContext ctx = SpringApplication.run(MarketTrackerApplication.class, args);
	
	
}
}